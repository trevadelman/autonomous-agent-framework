"""
This setup.py file is provided for backward compatibility with tools that don't
support pyproject.toml yet. The actual build configuration is in pyproject.toml.
"""

import setuptools

if __name__ == "__main__":
    setuptools.setup()
